# PyTorch（Manager 會依偵測結果替換 CUDA/CPU index）
# CPU 版本請改用: --extra-index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.10.0
torchvision==0.25.0
torchaudio==2.10.0

# Qwen3-ASR HF-native 與 GLM-OCR 共用 Transformers 5.x。
# Qwen3-ASR 於 2026-06-26 合入 Transformers；鎖官方 commit 避免浮動 main。
transformers @ git+https://github.com/huggingface/transformers.git
huggingface_hub
accelerate>=1.12.0
nagisa==0.2.11
soynlp==0.0.493

# 語者分離
pyannote.audio

# 音訊處理
numpy
soundfile
librosa
av

# 繁簡轉換
opencc-python-reimplemented

# Web 後端
fastapi
uvicorn[standard]
sqlalchemy
python-multipart

# YouTube 音頻下載
yt-dlp

# 環境變數
python-dotenv

# LLM Providers
openai
google-genai
PyJWT
pydantic[email]

# OCR（官方 SDK + HF-native Transformers 推論 + PDF）
-r requirements-ocr.txt
PyMuPDF
requests>=2.32.0

# 以圖搜頁 (CLIP)
Pillow

# Semantic (Embedding & Reranking)
FlagEmbedding 

ttkbootstrap
